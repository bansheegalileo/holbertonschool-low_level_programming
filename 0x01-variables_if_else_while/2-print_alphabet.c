#include <stdio.h>

/**
 * main - aye bee see dee eee eff jee ayech eye jay kay ell emm
 *
 * Return: Always (Success)
 */

int main(void)
{
	char c;
	for (c = 'a'; c <= 'z'; c++)
		putchar(c);

	putchar('\n');
	return (0);

}
