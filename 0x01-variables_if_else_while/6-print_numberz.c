#include <stdio.h>

/**
  * main - does kind of the same thing as 5
  *
  * Return: Always (Success)
  */

int main(void)
{
	int q;

	while (q <= 9; n++)
	{
		putchar(q);
	}

	putchar('\n');

	return (0);
}
